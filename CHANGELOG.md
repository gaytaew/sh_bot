# История изменений - Рефакторинг проекта

## Основные изменения

### ✅ Разделение на модули

Проект разделен на логические модули:

1. **`bot/`** - Telegram бот (UI слой)
   - `handlers/` - обработчики команд и callback
   - `keyboards.py` - все клавиатуры

2. **`services/`** - Бизнес-логика (отделена от UI)
   - `google_sheets.py` - работа с Google Sheets
   - `accounts.py` - создание и управление аккаунтами
   - `receipts.py` - генерация ссылок на квитанции
   - `address.py` - искажение и парсинг адресов
   - `ocr_gpt.py` - OCR и GPT обработка для eBay

3. **`models.py`** - Типизированные структуры данных
   - `AccountData` - данные аккаунта
   - `AddressParts` - компоненты адреса
   - `ReceiptData` - данные для квитанции
   - `eBayParsedData` - данные из eBay

4. **`constants.py`** - Константы и словари синонимов

### ✅ Учет различий между eBay и категориями

- **Категории**: адрес искажается перед записью в GS
  - Метод: `AccountService.create_account_from_category()`
  
- **eBay**: адрес НЕ искажается, записывается как есть от GPT
  - Метод: `AccountService.create_account_from_ebay()`

### ✅ Преимущества новой структуры

1. **Модульность**: каждый модуль отвечает за свою область
2. **Гибкость**: легко добавлять новые функции без изменения других
3. **Тестируемость**: сервисы можно тестировать отдельно
4. **Поддерживаемость**: изменения в одной функции не ломают другие

### 📝 Как использовать

Запуск бота теперь через:
```bash
python main.py
```

Старый файл `shokz_bot.py` переименован в `shokz_bot_old.py` как резервная копия.

### 🔄 Миграция

Все функции из старого кода перенесены в новые модули:
- Логика Google Sheets → `services/google_sheets.py`
- Создание аккаунтов → `services/accounts.py`
- Генерация квитанций → `services/receipts.py`
- Искажение адресов → `services/address.py`
- OCR/GPT → `services/ocr_gpt.py`
- Handlers → `bot/handlers/`

### ⚠️ Важно

- Конфигурация остается в `config.py`
- Все зависимости из `requirements.txt` остаются теми же
- Функциональность бота не изменилась, только структура кода

